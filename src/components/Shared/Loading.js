import React from 'react';

const Loading = () => {
    return (
        <div className='flex items-center justify-center'>
            <button className="btn loading w-28 mx-auto">loading</button>
        </div>
    );
};

export default Loading;